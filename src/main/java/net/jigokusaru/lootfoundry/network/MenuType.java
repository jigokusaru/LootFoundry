package net.jigokusaru.lootfoundry.network;

/**
 * A simple enum to represent the different UI screens we can open.
 * This makes our networking code much more readable and type-safe.
 */
public enum MenuType {
    MAIN,
    LOOT_EDITOR,
    OPTIONS
}